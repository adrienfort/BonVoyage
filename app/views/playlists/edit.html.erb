<div class="m-5">
  <h1>This the editation of <%= @playlist.name %></h1>
  <p>find me in app/views/playlists/edit.html.erb</p>

  <div class="m-3">
    <%= simple_form_for [ @explorer, @playlist ] do |f| %>
      <%= f.input :name %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
